package main

import (
	"testing"
)

func TestIndexedLineGetterWriteLine(t *testing.T) {
	/*file := `A
	BB
	CC
	DD`
		reader := strings.NewReader(file)
		index := strings.Builder{}
		output := strings.Builder{}

		// I'd test it here, but I need a memory buffer that implements ReadWriteSeeker for the index, and I cn't do that without an external dependency.

		lineWriter := NewIndexedLineWriter(reader, index, 1)
		lineWriter.WriteLine(1, output)
		if builder.String() != "A\n" {
			t.Error()
		}
	*/
}
